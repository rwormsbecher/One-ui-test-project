import React from "react";
import { Navbar, Commandbar } from "one-ui-fabric/dist";

const SettingsCommandBar = props => {
  return (
    <div className="navbar-auxiliary-row" style={{ height: "4rem" }}>
      &nbsp;
    </div>
  );
};

export default SettingsCommandBar;
